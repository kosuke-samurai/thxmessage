import { FC, ReactNode } from "react"
import Head from "next/head"

type Title = {
    title: string
    children: ReactNode
}

export const Layout:FC<Title> = ({children, title='THX Message'}) => {
  return (
      <div>
          <Head>
              <title>{ title}</title>
          </Head>
    </div>
  )
}
